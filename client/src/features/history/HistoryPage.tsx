// Placeholder — the history edit/delete view is built in milestone 6.
export function HistoryPage() {
  return (
    <div className="state">
      <strong style={{ color: 'var(--text)' }}>History</strong>
      <span className="muted">Coming in milestone 6 — view, edit, and delete logged sessions.</span>
    </div>
  )
}
